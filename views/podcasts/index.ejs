<h1>Podcasts</h1>
<div class"row">
  <% podcasts.forEach(podcast => { %>
      <div class="col-md-3">
      <img src="<%= podcast.image %>">
      <h4><%= podcast.name %></h4>
  </div>
  <% }); %>
</div>
